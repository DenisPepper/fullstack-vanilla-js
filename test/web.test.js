import { describe, it } from 'node:test';

describe('web app test suite', () => {
  it('Добавит новую строку в таблицу при валидных даных формы', async () => {
    console.log('working...');
  });
});
